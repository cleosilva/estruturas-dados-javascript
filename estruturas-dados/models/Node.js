export class Node {
    constructor(key){
        this.key = key; // valor do nó
        this.left = null; // referência ao nó filho da esquerda
        this.right = null; // referência ao nó filho da direita
    }
}